'use client';

import { Workspace } from '@/features/workspace/workspace';

/** Página do workspace multi-abas (núcleo operacional do Orbit). */
export default function WorkspacePage() {
  return (
    <div className="h-full">
      <Workspace />
    </div>
  );
}
